--- system/physmem.c	2024-07-09 00:56:54.535161845 +0000
+++ system/physmem-patched.c	2024-07-09 00:56:50.963086842 +0000
@@ -945,6 +945,8 @@
     unsigned long page, end;
 
     assert(start >= snap->start);
+    if (start + length > snap->end)
+        return false;
     assert(start + length <= snap->end);
 
     end = TARGET_PAGE_ALIGN(start + length - snap->start) >> TARGET_PAGE_BITS;
