--- hw/net/smc91c111.c.orig	2025-01-01 02:41:57.546162965 +0000
+++ hw/net/smc91c111.c	2025-01-05 05:12:58.438897161 +0000
@@ -237,6 +237,8 @@
         len = *(p++);
         len |= ((int)*(p++)) << 8;
         len -= 6;
+        if (len > 2042)
+            continue;
         control = p[len + 1];
         if (control & 0x20)
             len++;
@@ -428,7 +430,7 @@
             /* Ignore.  */
             return;
         case 2: /* Packet Number Register */
-            s->packet_num = value;
+            s->packet_num = value & (NUM_PACKETS - 1);
             return;
         case 3: case 4: case 5:
             /* Should be readonly, but linux writes to them anyway. Ignore.  */
@@ -454,6 +456,8 @@
                 } else {
                     p += (offset & 3);
                 }
+                if (p >= 2048)
+                    return;
                 s->data[n][p] = value;
             }
             return;
@@ -602,6 +606,8 @@
                 } else {
                     p += (offset & 3);
                 }
+                if (p >= 2048)
+                    return 0;
                 return s->data[n][p];
             }
         case 12: /* Interrupt status.  */
@@ -702,6 +708,8 @@
     packetnum = smc91c111_allocate_packet(s);
     if (packetnum == 0x80)
         return -1;
+    if (s->rx_fifo_len < 0 || s->rx_fifo_len >= NUM_PACKETS)
+        return -1;
     s->rx_fifo[s->rx_fifo_len++] = packetnum;
 
     p = &s->data[packetnum][0];
